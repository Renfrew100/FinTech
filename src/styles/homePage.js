import React from 'react';
import { StyleSheet } from "react-native"

export default StyleSheet.create({

    centerButtons: {
        marginTop: "auto",
        marginBottom: "auto",
        alignItems: "center"
    }
})